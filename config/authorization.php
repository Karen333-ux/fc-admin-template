<?php

declare(strict_types=1);

use Src\Contexts\Identity\Domain\Models\User;

return [

    /*
    |--------------------------------------------------------------------------
    | الحارس (Guard)
    |--------------------------------------------------------------------------
    | الـ Policy بتتنادى من Job و Command و API و Test — ومفيش لوحة Filament
    | مبنية في الحالات دي، فالحارس بييجي من هنا مش من filament(). (ADR-006)
    */
    'guard' => 'web',

    /*
    |--------------------------------------------------------------------------
    | موديل المستخدم
    |--------------------------------------------------------------------------
    | Src\Support ممنوع يستورد من Src\Contexts (ADR-011)، فالنواة المشتركة
    | (Tenant) بتوصل لموديل المستخدم من هنا بدل ما تستورده.
    */
    'user_model' => User::class,

    /*
    |--------------------------------------------------------------------------
    | فاصل المفتاح
    |--------------------------------------------------------------------------
    | الشكل النهائي للصلاحية: {action}.{resource}  مثال: view_any.users
    */
    'separator' => '.',

    /*
    |--------------------------------------------------------------------------
    | الأفعال القياسية لكل مورد
    |--------------------------------------------------------------------------
    */
    'default_actions' => [
        'view_any', 'view', 'create', 'update', 'delete',
        'delete_any', 'restore', 'force_delete', 'export',
    ],

    /*
    |--------------------------------------------------------------------------
    | الموارد
    |--------------------------------------------------------------------------
    | ⚠️ الشريحة الرأسية الأولى فيها مورد `users` **بس**. (docs/23 بند ١)
    |    أي مورد تاني بيتضاف بعد ما بوابة مراجعة العقود تعدّي (ADR-013).
    |
    | group   : مجموعة العرض في شاشة الأدوار (مفتاح ترجمة)
    | actions : لو فاضية → default_actions. لو محددة → دي بس.
    | extra   : أفعال مخصصة إضافية
    */
    'resources' => [

        'users' => [
            'group' => 'identity',
            'actions' => null,
            'extra' => ['impersonate', 'reset_password', 'force_logout'],
        ],

        // شريحة ٢ — المظهر والعام دلوقتي. manage_storage و manage_mail معرّفين
        // في الكتالوج (عشان الأدوار تتزامن) لكن صفحاتهم لسه ماتعملتش.
        //
        // صلاحية منفصلة لكل مجموعة إعدادات — `docs/05` بند ٧.
        'settings' => [
            'group' => 'system',
            'actions' => ['view_any', 'update'],
            'extra' => ['manage_general', 'manage_storage', 'manage_mail', 'manage_appearance'],
        ],

        // سجل النشاط — عرض بس، مفيش create/update/delete على سجل تدقيق.
        // `prune` مقصورة على super_admin (عبر `*` بس) — مش ضمن `admin` تحت.
        // (docs/11 بند ٤)
        'activity_logs' => [
            'group' => 'system',
            'actions' => ['view_any', 'view'],
            'extra' => ['prune'],
        ],

    ],

    /*
    |--------------------------------------------------------------------------
    | صلاحيات الصفحات المخصصة (مش مرتبطة بمورد)
    |--------------------------------------------------------------------------
    */
    'pages' => [
        // الدخول للوحة نفسها — بيتفحص في canAccessPanel().
        // ⚠️ من غير البند ده محدش غير super_admin يقدر يفتح اللوحة أصلاً. (ADR-003)
        'access.panel.admin' => 'system',
        'access.dashboard' => 'system',

        // فرض 2FA — قدرة صفحة برضه، مش صلاحية على مورد. الأدوار اللي
        // لازم تفعّل 2FA بتتحدد بمنحها القدرة دي تحت، مش بفحص دور مباشر
        // على المستخدم الفاعل (ممنوع في CLAUDE.md). (docs/12 بند ١)
        'require.two_factor' => 'system',

        // صفحة صحة النظام. (docs/11 بند ٧)
        //
        // ⚠️ كانت أصلاً موجودة في قايمة صلاحيات `admin` تحت من غير مدخل هنا —
        //    يعني كانت مرجع معلّق مالوش Gate ولا مزامنة حقيقية. الشريحة دي
        //    بتقفل الفجوة دي.
        'access.health' => 'system',
    ],

    /*
    |--------------------------------------------------------------------------
    | صلاحيات الودجتس
    |--------------------------------------------------------------------------
    | فاضية في الشريحة الأولى — الودجتس بره النطاق. (docs/23 بند ١)
    */
    'widgets' => [],

    /*
    |--------------------------------------------------------------------------
    | الأدوار الافتراضية
    |--------------------------------------------------------------------------
    | '*' = كل الصلاحيات. الأدوار دي بتتزامن مع كل تشغيل للأمر.
    |
    | الأنماط اللي بتشاور على موارد لسه ماتعملتش (roles, media, settings)
    | بتتوسّع لـ **لا شيء** — مقصود: الأدوار مكتوبة زي `docs/02` بالظبط عشان
    | ماتتغيّرش لما الموارد دي تتضاف.
    */
    'roles' => [
        'super_admin' => ['*'],

        // ملاحظة: الشق الشمال دايماً فعل، واليمين دايماً مورد (ADR-001).
        // «كل أفعال مورد» = *.users — مش users.*
        'admin' => [
            '*.users', '*.roles', '*.media', '*.settings',
            'view_any.activity_logs', 'view.activity_logs',
            'access.panel.admin', 'access.dashboard', 'access.health',
            'require.two_factor',
            'widget.*',
        ],

        'editor' => [
            'view_any.users', 'view.users',
            '*.media',
            'access.panel.admin', 'access.dashboard',
            'widget.stats_overview',
        ],

        'viewer' => [
            'view_any.*', 'view.*',
            'access.panel.admin', 'access.dashboard',
        ],
    ],

    /*
    |--------------------------------------------------------------------------
    | الدور الخارق
    |--------------------------------------------------------------------------
    | بيتجاوز الصلاحيات بس — مش قواعد السلامة ولا حدود المستأجر. (ADR-005)
    */
    'super_admin_role' => 'super_admin',

    /*
    |--------------------------------------------------------------------------
    | حماية من الحذف
    |--------------------------------------------------------------------------
    */
    'protected_roles' => ['super_admin'],
];
