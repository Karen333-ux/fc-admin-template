<?php

declare(strict_types=1);

return [

    /*
    |--------------------------------------------------------------------------
    | مدة الاحتفاظ (بالشهور)
    |--------------------------------------------------------------------------
    | أمر التقليم الدوري بيمسح أي سطر نشاط أقدم من كده — ما عدا
    | `log_name` = security أو financial، دول ثابتين مايتحذفوش أبداً.
    | (docs/11 بند ١٠)
    */
    'retention_months' => env('ACTIVITYLOG_RETENTION_MONTHS', 12),

];
