<?php

declare(strict_types=1);

/*
|--------------------------------------------------------------------------
| توكنز الثيم الثابتة
|--------------------------------------------------------------------------
| القيم دي **مش** إعدادات — المستأجر مايغيّرهاش. (docs/06 بند ٣: «ثابتة — متغيّرها»)
|
| اللون **الوحيد** اللي بيتغيّر هو لون العلامة، ومصدره `AppearanceSettings`
| (مع إمكانية دهسه لكل مستأجر عبر `TenantSettings`). مفيش مصدر تاني ليه.
|
| مكانها كونفيج مش كود عشان قاعدة CLAUDE.md بند ١: الألوان مصدرها كونفيج.
*/

return [

    // لون العلامة الاحتياطي — بيتستخدم بس لو الإعداد ناقص أو مش صالح.
    // المصدر الحقيقي هو AppearanceSettings::primary_color.
    'fallback_brand_color' => '#12454F',

    /*
    | ألوان الحالة — قيم الوضع الفاتح. (docs/06 بند ٣)
    |
    | ⚠️ قيم الوضع الداكن (#4ADE80 / #FBBF24 / #F87171) **ممنوعة في الفاتح**:
    |    تباينها على الأبيض 1.74:1 و1.67:1 و2.77:1 والحد الأدنى 3:1.
    */
    'status_colors' => [
        'success' => '#0F7A3D',
        'warning' => '#96550A',
        'danger' => '#B4232E',
        'info' => '#0E6E8C',
    ],

    // الحد الأدنى للتباين على الأبيض لدرجة ٦٠٠ (WCAG AA للنص العادي).
    // درجة ٧٠٠ أغمق فبتعدّي تلقائياً لو ٦٠٠ عدّت. (docs/06 بند ٢)
    'min_contrast_ratio' => 4.5,

    // ارتفاع اللوجو في الشريط العلوي
    'brand_logo_height' => '2.25rem',

    /*
    | مقاسات السايدبار — قيم ثابتة مش إعدادات. (docs/07 بند ٣)
    | المطوي بيسيب الأيقونات ظاهرة، فمقاسه مش صفر.
    */
    'sidebar' => [
        'width' => '17rem',
        'collapsed_width' => '4.5rem',
    ],

    'fonts' => [
        'sans' => 'IBM Plex Sans Arabic',
        'mono' => 'IBM Plex Mono',
    ],

];
